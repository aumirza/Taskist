@model Taskist.Models.Task

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>
    <hr />
<h3>Are you sure you want to delete the task @Model.Id</h3>
<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Content)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div class="d-flex gap-1">

        <input type="submit" value="Delete" class="btn btn-danger" /> 
        <a class="btn btn-primary mx-2" asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
